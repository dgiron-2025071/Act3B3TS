#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../webpack/bin/webpack.js" "$@"
else
  exec node  "$basedir/../../../webpack/bin/webpack.js" "$@"
fi
# cmd-shim-target=C:/Act31407/frontend/node_modules/.pnpm/webpack-subresource-integri_c39ce2506c4e7f65176d984ad1e10046/node_modules/webpack/bin/webpack.js
